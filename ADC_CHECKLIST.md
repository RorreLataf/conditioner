# Чеклист использования АЦП STM32G030F6P6

## ✅ Правильная последовательность инициализации

1. **Включить тактирование**
   - `RCC->IOPENR |= RCC_IOPENR_GPIOAEN;` (GPIO)
   - `RCC->AHBENR |= RCC_AHBENR_ADCEN;` (ADC)

2. **Настроить GPIO в аналоговый режим**
   - `GPIOA->MODER |= (0x3UL << (pin * 2));` (11 = аналоговый)
   - `GPIOA->PUPDR &= ~(0x3UL << (pin * 2));` (отключить подтяжки)

3. **Отключить ADC, если был включен**
   - Проверить `ADC1->CR & ADC_CR_ADEN`
   - Если включен: `ADC1->CR |= ADC_CR_ADDIS;`
   - Дождаться: `while (ADC1->CR & ADC_CR_ADEN);`

4. **Включить регулятор напряжения**
   - `ADC1->CR |= ADC_CR_ADVREGEN;`
   - **ВАЖНО**: Подождать минимум 10 мкс (~200 тактов при 16 МГц)

5. **Выполнить калибровку**
   - `ADC1->CR |= ADC_CR_ADCAL;`
   - Дождаться: `while (ADC1->CR & ADC_CR_ADCAL);`

6. **Настроить параметры**
   - Источник тактирования: `ADC1->CFGR2` (CKMODE)
   - Время выборки: `ADC1->SMPR` (SMP[2:0])
   - Канал: `ADC1->CHSELR` (CHSELx)
   - Режим: `ADC1->CFGR1` (CONT, RES, ALIGN)

7. **Включить ADC**
   - Сбросить флаг: `ADC1->ISR |= ADC_ISR_ADRDY;`
   - Включить: `ADC1->CR |= ADC_CR_ADEN;`
   - Дождаться: `while (!(ADC1->ISR & ADC_ISR_ADRDY));`

## ⚠️ Критические моменты

### Регулятор напряжения (ADVREGEN)
- **ДОЛЖЕН** быть включен перед калибровкой
- Требуется задержка ~10 мкс после включения
- Отключается автоматически при `ADDIS`

### Калибровка
- Выполняется **ОДИН РАЗ** при инициализации
- Нельзя пропускать - влияет на точность
- Занимает время (зависит от частоты)

### Время выборки (SMPR)
- Влияет на точность при высоком внутреннем сопротивлении источника
- Рекомендуется использовать максимальное (111 = 640.5 циклов) для точности
- Минимальное (000 = 2.5 цикла) только для низкоомных источников

### Выбор канала (CHSELR)
- Для одиночного канала установить только один бит CHSELx
- Для сканирования нескольких каналов установить несколько битов
- После изменения канала рекомендуется небольшая задержка

## 🔍 Типичные ошибки

1. ❌ **Пропуск включения ADVREGEN** - ADC не будет работать
2. ❌ **Недостаточная задержка после ADVREGEN** - нестабильная работа
3. ❌ **Пропуск калибровки** - неточные измерения
4. ❌ **Неправильная настройка GPIO** - сигнал не доходит до ADC
5. ❌ **Чтение DR до установки EOC** - старое значение
6. ❌ **Не сброс флага ADRDY перед ADEN** - может не включиться

## 📊 Регистры для отладки

- `ADC1->ISR` - флаги состояния (ADRDY, EOC, OVR, и т.д.)
- `ADC1->CR` - управление (ADEN, ADSTART, ADVREGEN, и т.д.)
- `ADC1->DR` - результат преобразования (12 бит)
- `ADC1->CHSELR` - выбранные каналы
- `ADC1->SMPR` - время выборки
- `ADC1->CFGR1` - конфигурация режима работы
- `ADC1->CFGR2` - конфигурация тактирования

## 🧪 Проверка работоспособности

1. Проверить, что `ADC1->ISR & ADC_ISR_ADRDY` установлен после инициализации
2. После `ADSTART` проверить, что `ADC1->ISR & ADC_ISR_EOC` устанавливается
3. Проверить, что `ADC1->DR` содержит разумные значения (0-4095)
4. При подключении известного напряжения проверить соответствие результата

## 📝 Пример минимальной проверки

```c
// После инициализации
if (!(ADC1->ISR & ADC_ISR_ADRDY)) {
    // Ошибка: ADC не готов
}

// После запуска преобразования
ADC1->CR |= ADC_CR_ADSTART;
uint32_t timeout = 10000;
while (!(ADC1->ISR & ADC_ISR_EOC) && timeout--) { __NOP(); }
if (timeout == 0) {
    // Ошибка: таймаут преобразования
}

uint16_t value = ADC1->DR & 0x0FFFU;
if (value > 4095) {
    // Ошибка: некорректное значение
}
```
